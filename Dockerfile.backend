FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

ENV NODE_ENV=production \
    PORT=3001 \
    HOST=0.0.0.0 \
    SERVE_FRONTEND=false

EXPOSE 3001

CMD ["npm", "start"]
